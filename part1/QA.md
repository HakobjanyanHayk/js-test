## Question/Answer of `Part 1`

# Why `.map()` is used instead of `.forEach()` for the second step?

## `.map()` is used instead of `.forEach()`, because `.map()` returns new array, and `.forEach()` affects original array 
